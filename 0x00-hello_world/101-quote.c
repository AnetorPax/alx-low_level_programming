#include <stdio.h>
/**
 * main - This program prints a string followed by
 * a new line to the standard error
 *
 * Return: 1 if successful.
 */
int main(void)
{
	fprintf(stderr, "and that piece of art is useful\"");
	fprintf(stderr, " - Dora Korpar, 2015-10-19\n");
	return (0);
}
